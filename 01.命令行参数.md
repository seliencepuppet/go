
```go
package main
// 命令行参数

import "os"
import "strings"
import "fmt"

func main(){
    who := "world!";
    if len(os.Args) > 1 {
        // os.Args[0] is CommandLine.exe 或者 CommandLine
        who = strings.Join(os.Args[1:], " ");
    }
    fmt.Printf("Hello", who);
}
```
